---
services:
  voxtera-backend:
    image: voxtera-backend:v1
    environment:
      USE_CUDA: false
    ports:
      - "8000:8000"
  voxtera-frontend:
    image: voxtera-frontend:v1
    environment:
      VITE_API_ROOT_URL: "http://localhost:8000"
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - voxtera-backend
